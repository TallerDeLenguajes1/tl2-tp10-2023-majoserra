@using EspacioTablero
@model MVC.ViewModel.ListarTableroViewModel
 <h2 class="text-center text-muted display-8 fw-light"> Mis Tableros</h2>
<table class="table table-striped table-hover">
    <thead class="table-dark">
        <tr>
            <td hidden>Id</td>
            <td hidden>IdTablero</td>
            <td>IdPropietario</td>
            <td>NombreTablero</td>
            <td>Descripcion</td>
            <td></td>
            <td></td>
        </tr>
    </thead>
    <tbody>

        @foreach (var mistablero in Model.MistablerosView)
        {
            <tr>
                <td hidden>@mistablero.Id</td>
                <td>@mistablero.Id_usuario_propetario</td>
                <td>@mistablero.NombreTablero</td>
                <td>@mistablero.Descripcion</td>
                <td><a class="btn btn-outline-dark" asp-controller="Tablero" asp-action="ModificarTablero" asp-route-id="@mistablero.Id">Modificar</a></td>
                <td>
                        <button type="button" class="btn btn-outline-danger" data-bs-toggle="modal" data-bs-target="#exampleModal-@mistablero.Id">
                            Eliminar
                        </button>

                        <div class="modal fade" id="exampleModal-@mistablero.Id" tabindex="-1" aria-labelledby="exampleModalLabel-@mistablero.Id"
                            aria-hidden="true">
                            <div class="modal-dialog">
                                <div class="modal-content">
                                    <div class="modal-header">
                                        <h1 class="modal-title fs-5" id="exampleModalLabel-@mistablero.Id">Eliminar Tablero</h1>
                                        <button type="button" class="btn-close" data-bs-dismiss="modal"
                                            aria-label="Close"></button>
                                    </div>
                                    <div class="modal-body">
                                        <p>¿Desea eliminar el tablero @mistablero.NombreTablero?</p>
                                    </div>
                                    <div class="modal-footer">
                                        <button type="button"  class="btn btn-dark" data-bs-dismiss="modal">Cancelar</button>
                                        <a class="btn  btn-outline-danger" asp-controller="Tablero" asp-action="EliminarTablero" asp-route-id="@mistablero.Id">Confirmar</a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </td>            
            </tr>
        }
    </tbody>
</table>

<h2 class="text-center text-muted display-8 fw-light">Listado de Tableros</h2>
<table class="table table-striped table-hover">
    <thead class="table-dark">
        <tr>
            <td hidden>Id</td>
            <td hidden>IdTablero</td>
            <td>IdPropietario</td>
            <td>NombreTablero</td>
            <td>Descripcion</td>
            <td></td>
            <td></td>
        </tr>
    </thead>
    <tbody>
        @foreach (var tablero in Model.TablerosView)
        {
            <tr>
                <td hidden>@tablero.Id</td>
                <td>@tablero.Id_usuario_propetario</td>
                <td>@tablero.NombreTablero</td>
                <td>@tablero.Descripcion</td>
                <td></td>
                <td></td>
                  </tr>
        }
    </tbody>
</table>



<div class="form-group mb-3 text-center">
    <div class="col-md-4 mx-auto"> <!-- Utiliza mx-auto para centrar en dispositivos medianos y grandes -->
        <a class="btn btn-dark btn-block text-white" asp-area="" asp-controller="Tablero" asp-action="CrearTablero">Agregar Tablero</a>
    </div>
</div>